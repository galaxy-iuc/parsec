<?xml version="1.0"?>
<tool id="parsec.workflows.export_workflow_to_local_path" name="workflows" version="1.0">
 <description>export_workflow_to_local_path</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="aggressive">
parsec workflows export_workflow_to_local_path
--workflow_id "$workflow_id"
--file_local_path "$file_local_path"

#if $use_default_filename
  --use_default_filename "$use_default_filename"
#end if

| jq -S . > $results
</command>
  <inputs>
    <!-- arguments -->
    <input name="workflow_id" label="workflow_id" argument="workflow_id"  type="text" help="Encoded workflow ID"/>
    <input name="file_local_path" label="file_local_path" argument="file_local_path"  type="text" help="Local path to which the exported file will be saved. (Should not contain filename if use_default_name=True)"/>

    <!-- options -->
    <input name="use_default_filename" argument="use_default_filename" type="boolean" truevalue="" falsevalue="" help="If the use_default_name parameter is True, the exported file will be saved as file_local_path/Galaxy-Workflow-%s.ga, where %s is the workflow name. If use_default_name is False, file_local_path is assumed to contain the full file path including filename."/>

  </inputs>
  <outputs>
    <data format="json" name="results"/>
  </outputs>
  <help>
Exports a workflow in JSON format to a given local path.
  </help>
</tool>
