<?xml version="1.0"?>
<tool id="parsec.libraries.upload_file_contents" name="libraries" version="1.0">
 <description>upload_file_contents</description>
	<macros>
		<import>macros.xml</import>
	</macros>
	<expand macro="requirements"/>
	<command detect_errors="aggressive">
parsec libraries upload_file_contents
"$library_id"
"$pasted_content"

#if $folder_id:
  --folder_id "$folder_id"
#end if
#if $file_type:
  --file_type "$file_type"
#end if
#if $dbkey:
  --dbkey "$dbkey"
#end if

| jq -S . > $results
</command>
	<inputs>
    <!-- arguments -->
	<param name="library_id" label="Library Id" argument="library_id" type="text" help="id of the library where to place the uploaded file" />
	<param name="pasted_content" label="Pasted Content" argument="pasted_content" type="text" help="Content to upload into the library" />

    <!-- options -->
	<param name="folder_id" label="Folder Id" argument="folder_id" type="text" help="id of the folder where to place the uploaded file. If not provided, the root folder will be used" />
	<param name="file_type" label="File Type" argument="file_type" type="text" help="Galaxy file format name" />
	<param name="dbkey" label="Dbkey" argument="dbkey" type="text" help="Dbkey" />

	</inputs>
	<outputs>
		<data format="json" name="results"/>
	</outputs>
	<help>
Upload pasted_content to a data library as a new file.
	</help>
</tool>
