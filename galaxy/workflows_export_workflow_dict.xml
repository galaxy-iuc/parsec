<?xml version="1.0"?>
<tool id="parsec.workflows.export_workflow_dict" name="workflows" version="1.0">
 <description>export_workflow_dict</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="aggressive">
parsec workflows export_workflow_dict
--workflow_id "$workflow_id"


| jq -S . > $results
</command>
  <inputs>
    <!-- arguments -->
    <input name="workflow_id" label="workflow_id" argument="workflow_id"  type="text" help="Encoded workflow ID"/>

    <!-- options -->

  </inputs>
  <outputs>
    <data format="json" name="results"/>
  </outputs>
  <help>
Exports a workflow.
  </help>
</tool>
