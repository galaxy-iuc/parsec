<?xml version="1.0"?>
<tool id="parsec.libraries.upload_from_galaxy_filesystem" name="libraries" version="1.0">
 <description>upload_from_galaxy_filesystem</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="aggressive">
parsec libraries upload_from_galaxy_filesystem
--library_id "$library_id"
--filesystem_paths "$filesystem_paths"

#if $folder_id
  --folder_id "$folder_id"
#end if
#if $file_type
  --file_type "$file_type"
#end if
#if $dbkey
  --dbkey "$dbkey"
#end if
#if $link_data_only
  --link_data_only "$link_data_only"
#end if
#if $roles
  --roles "$roles"
#end if

| jq -S . > $results
</command>
  <inputs>
    <!-- arguments -->
    <input name="library_id" label="library_id" argument="library_id"  type="text" help="id of the library where to place the uploaded file"/>
    <input name="filesystem_paths" label="filesystem_paths" argument="filesystem_paths"  type="text" help="file paths on the Galaxy server to upload to the library, one file per line"/>

    <!-- options -->
    <input name="folder_id" argument="folder_id" type="text" help="id of the folder where to place the uploaded files. If not provided, the root folder will be used"/>
    <input name="file_type" argument="file_type" type="text" help="Galaxy file format name"/>
    <input name="dbkey" argument="dbkey" type="text" help="Dbkey"/>
    <input name="link_data_only" argument="link_data_only" type="text" help="either 'copy_files' (default) or 'link_to_files'. Setting to 'link_to_files' symlinks instead of copying the files"/>
    <input name="roles" argument="roles" type="text" help="???"/>

  </inputs>
  <outputs>
    <data format="json" name="results"/>
  </outputs>
  <help>
Upload a set of files already present on the filesystem of the Galaxy server to a library.
  </help>
</tool>
