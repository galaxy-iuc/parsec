<?xml version="1.0"?>
<tool id="parsec.workflows.export_workflow_json" name="workflows" version="1.0">
 <description>export_workflow_json</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="aggressive">
parsec workflows export_workflow_json
--workflow_id "$workflow_id"


| jq -S . > $results
</command>
  <inputs>
    <!-- arguments -->
    <error />

    <!-- options -->

  </inputs>
  <outputs>
    <data format="json" name="results"/>
  </outputs>
  <help>
.. deprecated:: 0.9.0 Use :meth:`export_workflow_dict` instead.
  </help>
</tool>
