<?xml version="1.0"?>
<tool id="parsec.forms.get_forms" name="forms" version="1.0">
 <description>get_forms</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="aggressive">
parsec forms get_forms


| jq -S . > $results
</command>
  <inputs>
    <!-- arguments -->

    <!-- options -->

  </inputs>
  <outputs>
    <data format="json" name="results"/>
  </outputs>
  <help>
Get the list of all forms.
  </help>
</tool>
