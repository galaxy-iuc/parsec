<?xml version="1.0"?>
<tool id="parsec.folders.update_folder" name="folders" version="1.0">
 <description>update_folder</description>
	<macros>
		<import>macros.xml</import>
	</macros>
	<expand macro="requirements"/>
	<command detect_errors="aggressive">
parsec folders update_folder
"$folder_id"
"$name"

#if $description:
  --description "$description"
#end if

| jq -S . > $results
</command>
	<inputs>
    <!-- arguments -->
	<param name="folder_id" label="Folder Id" argument="folder_id" type="text" help="the folder's encoded id, prefixed by 'F'" />
	<param name="name" label="Name" argument="name" type="text" help="name of the new folder" />

    <!-- options -->
	<param name="description" label="Description" argument="description" type="text" help="folder's description" />

	</inputs>
	<outputs>
		<data format="json" name="results"/>
	</outputs>
	<help>
Update folder information.
	</help>
</tool>
