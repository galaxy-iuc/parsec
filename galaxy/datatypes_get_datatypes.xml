<?xml version="1.0"?>
<tool id="parsec.datatypes.get_datatypes" name="datatypes" version="1.0">
 <description>get_datatypes</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="aggressive">
parsec datatypes get_datatypes

#if $extension_only
  --extension_only "$extension_only"
#end if
#if $upload_only
  --upload_only "$upload_only"
#end if

| jq -S . > $results
</command>
  <inputs>
    <!-- arguments -->

    <!-- options -->
    <error />
    <error />

  </inputs>
  <outputs>
    <data format="json" name="results"/>
  </outputs>
  <help>
Get the list of all installed datatypes.
  </help>
</tool>
