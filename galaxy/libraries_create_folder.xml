<?xml version="1.0"?>
<tool id="parsec.libraries.create_folder" name="libraries" version="1.0">
 <description>create_folder</description>
	<macros>
		<import>macros.xml</import>
	</macros>
	<expand macro="requirements"/>
	<command detect_errors="aggressive">
parsec libraries create_folder
"$library_id"
"$folder_name"

#if $description:
  --description "$description"
#end if
#if $base_folder_id:
  --base_folder_id "$base_folder_id"
#end if

| jq -S . > $results
</command>
	<inputs>
    <!-- arguments -->
	<param name="library_id" label="Library Id" argument="library_id" type="text" help="library id to use" />
	<param name="folder_name" label="Folder Name" argument="folder_name" type="text" help="name of the new folder in the data library" />

    <!-- options -->
	<param name="description" label="Description" argument="description" type="text" help="description of the new folder in the data library" />
	<param name="base_folder_id" label="Base Folder Id" argument="base_folder_id" type="text" help="id of the folder where to create the new folder. If not provided, the root folder will be used" />

	</inputs>
	<outputs>
		<data format="json" name="results"/>
	</outputs>
	<help>
Create a folder in a library.
	</help>
</tool>
