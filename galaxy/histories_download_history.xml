<?xml version="1.0"?>
<tool id="parsec.histories.download_history" name="histories" version="1.0">
 <description>download_history</description>
	<macros>
		<import>macros.xml</import>
	</macros>
	<expand macro="requirements"/>
	<command detect_errors="aggressive">
parsec histories download_history
"$history_id"
"$jeha_id"
"$outf"

#if $chunk_size:
  --chunk_size "$chunk_size"
#end if

| jq -S . > $results
</command>
	<inputs>
    <!-- arguments -->
	<param name="history_id" label="History Id" argument="history_id" type="text" help="history ID" />
	<param name="jeha_id" label="Jeha Id" argument="jeha_id" type="text" help="jeha ID (this should be obtained via :meth:`export_history`)" />
	<param name="outf" label="Outf" argument="outf" type="data" format="data" help="output file object, open for writing in binary mode" />

    <!-- options -->
	<param name="chunk_size" label="Chunk Size" argument="chunk_size" type="integer" value="4096" help="how many bytes at a time should be read into memory" />

	</inputs>
	<outputs>
		<data format="json" name="results"/>
	</outputs>
	<help>
Download a history export archive.  Use :meth:`export_history` to create an export.
	</help>
</tool>
