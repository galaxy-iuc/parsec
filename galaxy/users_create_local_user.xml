<?xml version="1.0"?>
<tool id="parsec.users.create_local_user" name="users" version="1.0">
 <description>create_local_user</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="aggressive">
parsec users create_local_user
--username "$username"
--user_email "$user_email"
--password "$password"


| jq -S . > $results
</command>
  <inputs>
    <!-- arguments -->
    <param name="username" label="username" argument="username"  type="text" help="username of the user to be created"/>
    <param name="user_email" label="user_email" argument="user_email"  type="text" help="email of the user to be created"/>
    <param name="password" label="password" argument="password"  type="text" help="password of the user to be created"/>

    <!-- options -->

  </inputs>
  <outputs>
    <data format="json" name="results"/>
  </outputs>
  <help>
Create a new Galaxy local user.
  </help>
</tool>
