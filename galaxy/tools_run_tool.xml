<?xml version="1.0"?>
<tool id="parsec.tools.run_tool" name="tools" version="1.0">
 <description>run_tool</description>
	<macros>
		<import>macros.xml</import>
	</macros>
	<expand macro="requirements"/>
	<command detect_errors="aggressive">
parsec tools run_tool
"$history_id"
"$tool_id"
"$tool_inputs"


| jq -S . > $results
</command>
	<inputs>
    <!-- arguments -->
	<param name="history_id" label="History Id" argument="history_id" type="text" help="encoded ID of the history in which to run the tool" />
	<param name="tool_id" label="Tool Id" argument="tool_id" type="text" help="ID of the tool to be run" />
	<param name="tool_inputs" label="Tool Inputs" argument="tool_inputs" type="data" format="json" help="dictionary of input datasets and parameters for the tool (see below)" />

    <!-- options -->

	</inputs>
	<outputs>
		<data format="json" name="results"/>
	</outputs>
	<help>
Runs tool specified by ``tool_id`` in history indicated by ``history_id`` with inputs from ``dict`` ``tool_inputs``.
	</help>
</tool>
