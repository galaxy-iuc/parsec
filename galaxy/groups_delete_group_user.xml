<?xml version="1.0"?>
<tool id="parsec.groups.delete_group_user" name="groups" version="1.0">
 <description>delete_group_user</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="aggressive">
parsec groups delete_group_user
--group_id "$group_id"
--user_id "$user_id"


| jq -S . > $results
</command>
  <inputs>
    <!-- arguments -->
    <param name="group_id" label="group_id" argument="group_id"  type="text" help="Encoded group ID"/>
    <param name="user_id" label="user_id" argument="user_id"  type="text" help="Encoded user ID to remove from the group"/>

    <!-- options -->

  </inputs>
  <outputs>
    <data format="json" name="results"/>
  </outputs>
  <help>
Remove a user from the given group.
  </help>
</tool>
