<?xml version="1.0"?>
<tool id="parsec.jobs.search_jobs" name="jobs" version="1.0">
 <description>search_jobs</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="aggressive">
parsec jobs search_jobs
--job_info "$job_info"


| jq -S . > $results
</command>
  <inputs>
    <!-- arguments -->
    <param name="job_info" label="job_info" argument="job_info"  type="data" format="json" help="dictionary containing description of the requested job. This is in the same format as a request to POST /api/tools would take to initiate a job"/>

    <!-- options -->

  </inputs>
  <outputs>
    <data format="json" name="results"/>
  </outputs>
  <help>
Return jobs for the current user based payload content.
  </help>
</tool>
