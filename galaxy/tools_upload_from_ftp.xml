<?xml version="1.0"?>
<tool id="parsec.tools.upload_from_ftp" name="tools" version="1.0">
 <description>upload_from_ftp</description>
	<macros>
		<import>macros.xml</import>
	</macros>
	<expand macro="requirements"/>
	<command detect_errors="aggressive">
parsec tools upload_from_ftp
"$path"
"$history_id"


| jq -S . > $results
</command>
	<inputs>
    <!-- arguments -->
	<param name="path" label="Path" argument="path" type="text" help="path of the file in the user's FTP directory" />
	<param name="history_id" label="History Id" argument="history_id" type="text" help="id of the history where to upload the file" />

    <!-- options -->

	</inputs>
	<outputs>
		<data format="json" name="results"/>
	</outputs>
	<help>
Upload the file specified by ``path`` from the user's FTP directory to the history specified by ``history_id``.
	</help>
</tool>
