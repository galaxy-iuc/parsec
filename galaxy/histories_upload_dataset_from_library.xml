<?xml version="1.0"?>
<tool id="parsec.histories.upload_dataset_from_library" name="histories" version="1.0">
 <description>upload_dataset_from_library</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="aggressive">
parsec histories upload_dataset_from_library
--history_id "$history_id"
--lib_dataset_id "$lib_dataset_id"


| jq -S . > $results
</command>
  <inputs>
    <!-- arguments -->
    <param name="history_id" label="history_id" argument="history_id"  type="text" help="Encoded history ID"/>
    <param name="lib_dataset_id" label="lib_dataset_id" argument="lib_dataset_id"  type="text" help="Encoded library dataset ID"/>

    <!-- options -->

  </inputs>
  <outputs>
    <data format="json" name="results"/>
  </outputs>
  <help>
Upload a dataset into the history from a library. Requires the library dataset ID, which can be obtained from the library contents.
  </help>
</tool>
