<?xml version="1.0"?>
<tool id="parsec.libraries.delete_library_dataset" name="libraries" version="1.0">
 <description>delete_library_dataset</description>
	<macros>
		<import>macros.xml</import>
	</macros>
	<expand macro="requirements"/>
	<command detect_errors="aggressive">
parsec libraries delete_library_dataset
"$library_id"
"$dataset_id"

#if $purged:
  $purged
#end if

| jq -S . > $results
</command>
	<inputs>
    <!-- arguments -->
	<param name="library_id" label="library_id" argument="library_id" type="text" help="library id where dataset is found in" />
	<param name="dataset_id" label="dataset_id" argument="dataset_id" type="text" help="id of the dataset to be deleted" />

    <!-- options -->
	<param name="purged" label="purged" argument="purged" type="booelan" truevalue="--purged" falsevalue="" help="Indicate that the dataset should be purged (permanently deleted)" />

	</inputs>
	<outputs>
		<data format="json" name="results"/>
	</outputs>
	<help>
Delete a library dataset in a data library.
	</help>
</tool>
