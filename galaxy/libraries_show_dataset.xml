<?xml version="1.0"?>
<tool id="parsec.libraries.show_dataset" name="libraries" version="1.0">
 <description>show_dataset</description>
	<macros>
		<import>macros.xml</import>
	</macros>
	<expand macro="requirements"/>
	<command detect_errors="aggressive">
parsec libraries show_dataset
"$library_id"
"$dataset_id"


| jq -S . > $results
</command>
	<inputs>
    <!-- arguments -->
	<param name="library_id" label="library_id" argument="library_id" type="text" help="library id where dataset is found in" />
	<param name="dataset_id" label="dataset_id" argument="dataset_id" type="text" help="id of the dataset to be inspected" />

    <!-- options -->

	</inputs>
	<outputs>
		<data format="json" name="results"/>
	</outputs>
	<help>
Get details about a given library dataset. The required ``library_id`` can be obtained from the datasets's library content details.
	</help>
</tool>
