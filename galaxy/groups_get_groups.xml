<?xml version="1.0"?>
<tool id="parsec.groups.get_groups" name="groups" version="1.0">
 <description>get_groups</description>
	<macros>
		<import>macros.xml</import>
	</macros>
	<expand macro="requirements"/>
	<command detect_errors="aggressive">
parsec groups get_groups


| jq -S . > $results
</command>
	<inputs>
    <!-- arguments -->

    <!-- options -->

	</inputs>
	<outputs>
		<data format="json" name="results"/>
	</outputs>
	<help>
Get all (not deleted) groups.
	</help>
</tool>
